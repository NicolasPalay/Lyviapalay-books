{% extends 'base.html.twig' %}

{% block title %}Mon compte{% endblock %}

{% block body %}
    <h1 class="text-center">Mon compte</h1>
<div class="container">
    Bienvenue  dans votre espace personnel <strong>{{ app.user.firstName }}</strong>.
<br>
    adresse mail : {{ app.user.email }}
<br>
        mes adresses  : <a href="{{ path('account_address') }}">mes adresses </a>
    <br>
    mes commande  : <a href="{{ path('account_order') }}">mes commandes </a>
    <br>
    <br>
    <a href="{{ path('account_password') }}">Modifier mon mot de passe</a>
    <br>
    <a href="{{ path('app_logout') }}">Déconnexion</a>
</div>
{% endblock %}
