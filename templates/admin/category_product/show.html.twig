{% extends 'baseAdmin.html.twig' %}

{% block title %}CategoryProduct{% endblock %}

{% block body %}
    <h1>CategoryProduct</h1>
    <ul>

    </ul>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category_product.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category_product.name }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ category_product.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_category_product_index') }}">back to list</a>

    <a href="{{ path('admin_category_product_edit', {'id': category_product.id}) }}">edit</a>

    {{ include('admin/category_product/_delete_form.html.twig') }}
{% endblock %}
